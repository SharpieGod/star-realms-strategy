(
    name: ImperialFrigate,
    faction: StarEmpire,
    card_type: Ship,
    cost: 3,
    effect: Sequence([
        Resource(Combat, 4),
        OpponentDiscards,
        If(HasAlly(StarEmpire), Resource(Combat, 2)),
        On(Scrap, Draw(Number(1))),
    ]),
)

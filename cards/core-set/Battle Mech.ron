(
    name: BattleMech,
    faction: MachineCult,
    card_type: Ship,
    cost: 5,
    effect: Sequence([
        Base(Resource(Combat, 4)),
        May(Base(Scrap(HandOrDiscardPile, 1))),
        If(HasAlly(MachineCult), Draw(Number(1))),
    ]),
)

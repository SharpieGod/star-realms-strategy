(
    name: "Blob Destroyer",
    faction: Blob,
    card_type: Ship,
    cost: 4,
    effect: Sequence([
        TargetedEffect(Resource(Attack, 6)),
        If(HasAlly(Blob), May(AndOr(TargetedEffect(DestroyTargetBase), TargetedEffect(ScrapCardInRow)))),
    ]),
)

(
    name: SurveyShip,
    faction: StarEmpire,
    card_type: Ship,
    cost: 3,
    effect: Sequence([
        Resource(Trade, 1),
        Draw(Number(1)),
        On(Scrap, OpponentDiscards),
    ]),
)

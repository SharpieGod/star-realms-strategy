(
    name: BattleBlob,
    faction: Blob,
    card_type: Ship,
    cost: 6,
    effect: Sequence([
        Resource(Combat, 8),
        If(HasAlly(Blob), Draw(Number(1))),
        On(Scrap, Resource(Combat, 4)),
    ]),
)

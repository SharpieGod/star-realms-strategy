(
    name: BattleBlob,
    faction: Blob,
    card_type: Ship,
    cost: 6,
    effect: Sequence([
        Base(Resource(Combat, 8)),
        If(HasAlly(Blob), Draw(Number(1))),
        On(Scrap, Base(Resource(Combat, 4))),
    ]),
)

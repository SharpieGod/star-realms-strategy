(
    name: FederationShuttle,
    faction: TradeFederation,
    card_type: Ship,
    cost: 1,
    effect: Sequence([
        Base(Resource(Trade, 2)),
        If(HasAlly(TradeFederation), Base(Resource(Authority, 4))),
    ]),
)

(
    name: EmbassyYacht,
    faction: TradeFederation,
    card_type: Ship,
    cost: 3,
    effect: Sequence([
        Base(Resource(Authority, 3)),
        Base(Resource(Trade, 2)),
        If(BaseCountAtLeast(2), Draw(Number(2))),
    ]),
)

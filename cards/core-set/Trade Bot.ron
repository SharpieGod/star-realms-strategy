(
    name: TradeBot,
    faction: MachineCult,
    card_type: Ship,
    cost: 1,
    effects: [
        Resource(Trade, 1),
        May(Scrap(HandOrDiscardPile, 1)),
        If(HasAlly(MachineCult), Resource(Combat, 2)),
    ],
)

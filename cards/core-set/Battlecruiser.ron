(
    name: Battlecruiser,
    faction: StarEmpire,
    card_type: Ship,
    cost: 6,
    effects: [
        Resource(Combat, 5),
        Draw(Number(1)),
        If(HasAlly(StarEmpire), OpponentDiscards),
        On(Scrap, Sequence([
            Draw(Number(1)),
            May(DestroyTargetBase),
        ])),
    ],
)

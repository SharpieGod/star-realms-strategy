(
    name: ImperialFighter,
    faction: StarEmpire,
    card_type: Ship,
    cost: 1,
    effect: Sequence([
        Base(Resource(Combat, 2)),
        Base(OpponentDiscards),
        If(HasAlly(StarEmpire), Base(Resource(Combat, 2))),
    ]),
)

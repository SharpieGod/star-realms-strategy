(
    name: PatrolMech,
    faction: MachineCult,
    card_type: Ship,
    cost: 4,
    effects: [
        Or(Resource(Trade, 3), Resource(Combat, 5)),
        If(HasAlly(MachineCult), May(Scrap(HandOrDiscardPile, 1))),
    ],
)

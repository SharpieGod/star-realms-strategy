(
    name: MissileMech,
    faction: MachineCult,
    card_type: Ship,
    cost: 6,
    effect: Sequence([
        Resource(Combat, 6),
        May(DestroyTargetBase),
        If(HasAlly(MachineCult), Draw(Number(1))),
    ]),
)

(
    name: Ram,
    faction: Blob,
    card_type: Ship,
    cost: 3,
    effects: [
        Resource(Combat, 5),
        If(HasAlly(Blob), Resource(Combat, 2)),
        ScrapAbility(Resource(Trade, 3)),
    ],
)

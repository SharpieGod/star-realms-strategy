(
    name: BarterWorld,
    faction: TradeFederation,
    card_type: Base(defense: 4, it_outpost: false),
    cost: 4,
    effects: [
        Or(Resource(Authority, 2), Resource(Trade, 2)),
        On(Scrap, Resource(Combat, 5)),
    ],
)

(
    name: CommandShip,
    faction: TradeFederation,
    card_type: Ship,
    cost: 8,
    effects: [
        Resource(Authority, 4),
        Resource(Combat, 5),
        Draw(Number(2)),
        // Core Set.csv lists this as "Destroy target base." (no "may"), but the
        // actual printed card text is "You may destroy target base."
        If(HasAlly(TradeFederation), May(DestroyTargetBase)),
    ],
)
